import SwiftUI
import AppKit

struct CIColor {
    static var white = Color.white
    static var text = Color.black
    static var highlight = Color(NSColor.controlAccentColor)
    static var background = Color(NSColor.windowBackgroundColor)
    static var link = Color(NSColor.linkColor)
    static var ok = Color.green
    static var warning = Color.red
}
